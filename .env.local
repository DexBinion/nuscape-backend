# Frontend environment - keep only frontend-specific values here
VITE_PROXY=1
VITE_API_BASE=http://localhost:5001

# NOTE: DATABASE_URL should live only in the repository-root .env (backend uses that).
# Removing DATABASE_URL from frontend env to avoid leaking DB credentials to the client.